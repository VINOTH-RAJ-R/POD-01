1. Copy Makefile.sample to Makefile and set the correct paths in it
2. Run 'make' and 'make install'
3. Issue the command 'CREATE EXTENSION markednullcore' to your PostgreSQL instance to load the extension
